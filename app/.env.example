# Copie para .env.local e preencha com as chaves do projeto Supabase
# (whjfaukvqutgmyozfesb). Nunca commite .env.local — já está no .gitignore.
#
# Sem estas duas variáveis o app funciona normalmente, só que 100% local
# (localStorage), sem sincronizar entre aparelhos.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
